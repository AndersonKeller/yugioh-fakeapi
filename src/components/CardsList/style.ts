import styled from "styled-components";

export const StyledList = styled.ul`
  display: flex;

  overflow-x: scroll;
  gap: 1rem;
  padding: 1.5rem;
  width: 100vw;
  max-width: 1100px;
  margin: 0 auto;
`;
